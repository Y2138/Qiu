基于以上的分析，我认为 PromptPreset 和 workflowTemplate 实在是过度设计了，首先这两个不应该影响 tools 的使用，并且现在 register 中不同的 promptPreset 甚至包含同样的 tools，开启多个会造成混乱，当前各自的 register 注册的数据结构也太复杂，统一简化实现，按照以下的原则：
1. 移除 workflowTemplate 概念，最简化promptPreset配置，例如只是一段提示词，不要影响到 agent tools 的策略
2. 移除 skill 的功能，因为风险很难管控
3. 简化 agent 的工作流程
先帮我分析下，现在的 workflowTemplate 的 stage 概念会影响到 agent 行为吗，又是如何影响的；再评估下我这个改造方案是